name: Update README with Recent Features

on:
  push:
    branches: [main, master]
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

jobs:
  update-readme:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 100

      - name: Update README with recent features
        run: |
          COMMITS=$(git log --oneline --grep="^feat" --pretty=format:'| %s | %ad | [%h](https://github.com/goldbarth/ArcFlow/commit/%H) |' --date=format:'%Y-%m-%d %H:%M:%S CET' -n 5)
          
          if [ -z "$COMMITS" ]; then
            echo "No feat commits found"
            exit 0
          fi
          
          TABLE="| Feature | Commit Date | Commit |
          |---------|-------------|--------|
          $COMMITS"
          
          awk -v table="$TABLE" '
            /<!-- START_RECENTLY_WORKED_ON -->/ {
              print
              print table
              skip = 1
              next
            }
            /<!-- END_RECENTLY_WORKED_ON -->/ {
              skip = 0
            }
            !skip { print }
          ' README.md > README.tmp && mv README.tmp README.md

      - name: Commit and push changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add README.md
          git diff --staged --quiet || git commit -m "docs: update recently worked on section"
          git push
